FROM ghcr.io/oki602960/nanobackend:latest

# Expose the port the application runs on
EXPOSE 8080

# Command to run the application (if not already defined in the base image)
# CMD ["your-application-command-here"]
